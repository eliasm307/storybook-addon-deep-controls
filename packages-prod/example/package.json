{
  "name": "example-app-prod",
  "private": true,
  "scripts": {
    "storybook": "echo 'Starting storybook' && npx storybook dev -p 6006 --no-open",
    "build-storybook": "storybook build"
  },
  "installConfig": {
    "hoistingLimits": "workspaces"
  },
  "dependencies": {
    "storybook-addon-deep-controls": "^0.1.1"
  }
}
