# todo make vite package the published one
# see https://docs.netlify.com/configure-builds/file-based-configuration/#configuration-details
# All paths configured in the netlify.toml should be absolute paths relative to the base directory, which is the root by default (/). I assume this takes the folder with the .git folder as the base directory.
[build]
  # see https://docs.netlify.com/configure-builds/file-based-configuration/#build-settings
  command = npm run build
  publish = "./packages/example-v8/storybook-static"